

      <ImageBackground
            source={{ uri }}
            style={{
              width,
              height,
            }}
            resizeMode="cover"
          >
            
          </ImageBackground>

          <Image
            source={uri}
            style={{
              width,
              height,
              position: "absolute",
              top: 0,
              start: 0,
            }}
            contentFit="contain"
            placeholder={null}
            transition={0}
          />
import React from 'react'
import { SafeAreaView, Text } from 'react-native'

export default function VideoPlayer() {
  return (
    <SafeAreaView><Text>VideoPlayer</Text></SafeAreaView>
  )
}

